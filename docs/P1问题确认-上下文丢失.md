# 上下文丢失问题实际影响分析

**时间**: 2026-01-17
**问题类型**: 功能缺失
**严重程度**: P1 高

---

## 问题确认

### 默认配置

```typescript
// src/context/types.ts:109-111
export const DEFAULT_CONTEXT_CONFIG = {
  storagePath: '.multicli/context',
  immediateContextRounds: 5,  // 只保留最近5轮对话
  // ...
}
```

**实际限制**:
- 每轮对话 = 1条用户消息 + 1条助手消息
- 5轮对话 = 10条消息
- 超过10条消息后，最早的消息被丢弃

### 当前实现

```typescript
// src/context/context-manager.ts:88-100
addMessage(message, applyTruncation = true) {
  this.immediateContext.push(message);

  // 保持即时上下文在限定轮数内
  const maxMessages = this.config.immediateContextRounds * 2;  // 10条
  if (this.immediateContext.length > maxMessages) {
    const systemMessages = this.immediateContext.filter(m => m.role === 'system');
    const otherMessages = this.immediateContext.filter(m => m.role !== 'system');
    this.immediateContext = [
      ...systemMessages,
      ...otherMessages.slice(-maxMessages)  // ❌ 旧消息直接丢弃
    ];
  }

  // ❌ 没有迁移到 Memory 的代码！
}
```

**问题**: 使用 `.slice(-maxMessages)` 直接丢弃旧消息，**未迁移到 SessionMemory**

---

## 实际影响场景

### 场景1: 长会话任务链

**用户对话**:
```
第1轮: "创建一个用户管理系统"
      → 助手创建 User, UserService, UserController

第2轮: "添加权限管理功能"
      → 助手创建 Permission, Role, RBAC

第3轮: "集成数据库"
      → 助手配置 TypeORM, 创建 migrations

第4轮: "添加用户注册和登录API"
      → 助手实现 AuthController, JWT

第5轮: "添加邮件验证功能"
      → 助手集成 nodemailer, 创建 EmailService

第6轮: "添加密码重置功能"  ← 此时第1轮的上下文被丢弃
      → 助手: "什么是用户管理系统？请提供更多上下文"  ❌
```

**问题**: 第6轮时，编排者**忘记了**第1轮创建的User系统架构

### 场景2: 需求迭代

**用户对话**:
```
第1轮: "使用 React + TypeScript 创建前端"
      决策: 使用函数组件 + Hooks

第2轮: "添加路由系统"
第3轮: "添加状态管理"
第4轮: "添加表单验证"
第5轮: "添加国际化"
第6轮: "创建用户列表页面"  ← 第1轮的技术栈决策被丢弃
      → 助手可能使用 class 组件，与之前不一致  ❌
```

**问题**: 早期的**关键技术决策**被遗忘，导致代码风格不一致

### 场景3: 错误修复链

**用户对话**:
```
第1轮: "发现 TypeError: Cannot read property 'name'"
      → 助手分析: User对象可能为null

第2轮: "添加null检查"
第3轮: "但现在报错 'id' is undefined"
第4轮: "修复id问题"
第5轮: "现在登录失败"
第6轮: "为什么登录失败？"  ← 第1轮的根本问题被丢弃
      → 助手: "请提供错误日志" (忘记了最初的TypeError)  ❌
```

**问题**: **错误调试的历史**被遗忘，重复排查相同问题

---

## Memory 系统现状

### SessionMemory 的设计

```typescript
// src/context/memory-document.ts
export interface MemoryContent {
  currentTasks: Array<{
    id: string;
    description: string;
    status: 'pending' | 'in_progress';
    assignedWorker?: string;
  }>;
  completedTasks: Array<{
    id: string;
    description: string;
    result?: string;
  }>;
  keyDecisions: Array<{
    id: string;
    description: string;
    reason: string;
  }>;
  importantContext: string[];
  codeChanges: Array<{
    file: string;
    action: 'add' | 'modify' | 'delete';
    summary: string;
  }>;
  pendingIssues: string[];
  tokenEstimate: number;
}
```

**Memory 的优点**:
- ✅ 结构化存储关键信息
- ✅ 可以持久化到磁盘
- ✅ Token占用小（只存储摘要）

**Memory 的现状**:
- ❌ 只有手动添加接口，无自动迁移
- ❌ ContextManager 从不调用 `sessionMemory.addXXX()`
- ❌ 即时上下文溢出时，信息完全丢失

---

## 触发概率评估

### 高概率触发

1. **普通开发任务**: 5-10轮对话很常见
   - 创建功能 + 测试 + 修复 + 优化 = 至少6轮
   - 触发概率: **80%**

2. **架构设计任务**: 10-20轮对话
   - 需求讨论 + 方案选择 + 实现 + 集成
   - 触发概率: **100%**

3. **Bug调试**: 5-15轮对话
   - 分析 + 假设 + 验证 + 修复
   - 触发概率: **90%**

### 实际影响

**用户体验**:
- ❌ 助手"失忆"，重复询问已解答的问题
- ❌ 新实现与早期决策不一致
- ❌ 重复犯同样的错误

**数据损失**:
- ❌ 早期需求丢失
- ❌ 技术决策丢失
- ❌ 错误历史丢失

**综合评估**: 触发概率 **80%**，影响严重

---

## 修复方案

### 方案1: 自动迁移（推荐）

```typescript
// src/context/context-manager.ts

addMessage(message: Omit<ContextMessage, 'timestamp'>, applyTruncation = true) {
  let content = message.content;

  // Augment 风格截断
  if (applyTruncation && this.config.compression.truncation.enabled) {
    const truncated = this.truncationUtils.truncateMessage(content);
    if (truncated.wasTruncated) {
      content = truncated.content;
    }
  }

  const msg: ContextMessage = {
    ...message,
    content,
    timestamp: new Date().toISOString(),
    tokenCount: this.estimateTokens(content)
  };

  this.immediateContext.push(msg);

  // 🆕 检查是否需要迁移
  const maxMessages = this.config.immediateContextRounds * 2;
  if (this.immediateContext.length > maxMessages) {
    // 🆕 Step 1: 提取将被移除的消息
    const systemMessages = this.immediateContext.filter(m => m.role === 'system');
    const otherMessages = this.immediateContext.filter(m => m.role !== 'system');
    const toRemove = otherMessages.slice(0, -maxMessages);

    // 🆕 Step 2: 迁移到 Memory
    for (const removedMsg of toRemove) {
      this.migrateToMemory(removedMsg);
    }

    // Step 3: 清理即时上下文
    this.immediateContext = [
      ...systemMessages,
      ...otherMessages.slice(-maxMessages)
    ];

    console.log(
      `[ContextManager] 即时上下文已清理，迁移 ${toRemove.length} 条消息到 Memory`
    );
  }
}

/**
 * 🆕 新增方法: 将消息迁移到 SessionMemory
 */
private migrateToMemory(message: ContextMessage): void {
  if (!this.sessionMemory) {
    console.warn('[ContextManager] SessionMemory 未初始化，无法迁移消息');
    return;
  }

  // 1. 用户消息 → 提取需求和决策
  if (message.role === 'user') {
    const content = message.content;

    // 检测是否包含技术决策
    if (this.containsTechnicalDecision(content)) {
      this.sessionMemory.addDecision({
        id: `decision-${Date.now()}`,
        description: this.extractDecisionSummary(content),
        reason: '用户需求',
      });
    }

    // 检测是否包含重要需求
    if (content.length > 50) {
      this.sessionMemory.addImportantContext(
        `用户需求: ${content.substring(0, 200)}${content.length > 200 ? '...' : ''}`
      );
    }
  }

  // 2. 助手消息 → 提取任务和变更
  if (message.role === 'assistant') {
    const content = message.content;

    // 提取已完成任务
    const taskMatches = content.match(/(?:完成|创建|实现|修改|添加)(?:了)?[:：]\s*([^\n]{10,100})/g);
    if (taskMatches && taskMatches.length > 0) {
      for (const match of taskMatches.slice(0, 3)) {  // 最多提取3个任务
        this.sessionMemory.addCompletedTask({
          id: `task-${Date.now()}-${Math.random()}`,
          description: match.replace(/[:：]/g, ': ').substring(0, 100),
        });
      }
    }

    // 提取文件变更
    const fileMatches = content.match(/(?:创建|修改|删除)(?:了)?\s+[`']?([a-zA-Z0-9\-_/\.]+\.[a-z]+)[`']?/g);
    if (fileMatches && fileMatches.length > 0) {
      for (const match of fileMatches.slice(0, 5)) {  // 最多提取5个文件
        const actionMatch = match.match(/(创建|修改|删除)/);
        const fileMatch = match.match(/([a-zA-Z0-9\-_/\.]+\.[a-z]+)/);
        if (actionMatch && fileMatch) {
          const action = this.mapActionType(actionMatch[1]);
          this.sessionMemory.addCodeChange({
            file: fileMatch[1],
            action,
            summary: match.substring(0, 100),
          });
        }
      }
    }

    // 提取关键结论
    const conclusionMatches = content.match(/(?:因此|所以|结论|总结)[:：]\s*([^\n]{10,200})/g);
    if (conclusionMatches && conclusionMatches.length > 0) {
      for (const match of conclusionMatches.slice(0, 2)) {
        this.sessionMemory.addImportantContext(
          `关键结论: ${match.replace(/[:：]/g, ': ')}`
        );
      }
    }
  }
}

/**
 * 检测内容是否包含技术决策
 */
private containsTechnicalDecision(content: string): boolean {
  const decisionKeywords = [
    '使用', '选择', '采用', '决定',
    'use', 'choose', 'adopt', 'decide',
    'React', 'TypeScript', 'Vue', 'Angular',
    '数据库', 'PostgreSQL', 'MySQL', 'MongoDB',
    '架构', 'MVC', 'MVVM', 'Microservices'
  ];

  return decisionKeywords.some(keyword =>
    content.toLowerCase().includes(keyword.toLowerCase())
  );
}

/**
 * 提取决策摘要
 */
private extractDecisionSummary(content: string): string {
  // 简单实现：提取包含决策关键词的句子
  const sentences = content.split(/[。.!！\n]/);
  for (const sentence of sentences) {
    if (this.containsTechnicalDecision(sentence)) {
      return sentence.trim().substring(0, 200);
    }
  }
  return content.substring(0, 200);
}

/**
 * 映射操作类型
 */
private mapActionType(action: string): 'add' | 'modify' | 'delete' {
  if (action.includes('创建') || action.includes('新增')) return 'add';
  if (action.includes('删除') || action.includes('移除')) return 'delete';
  return 'modify';
}
```

### 方案2: 智能压缩（可选，长期）

```typescript
/**
 * 🆕 使用 LLM 压缩 Memory
 */
async compressMemory(): Promise<void> {
  if (!this.sessionMemory) return;

  const content = this.sessionMemory.getContent();
  const currentTokens = content.tokenEstimate;

  // 只在超过阈值时压缩
  if (currentTokens < this.config.compression.tokenLimit) {
    return;
  }

  console.log(`[ContextManager] Memory 大小: ${currentTokens} tokens，开始压缩...`);

  // 使用 Claude 压缩任务历史
  const taskSummary = await this.compressTaskHistory(content.completedTasks);
  const decisionSummary = await this.compressDecisions(content.keyDecisions);

  // 替换原始数据
  this.sessionMemory.replaceCompletedTasks([{
    id: 'summary',
    description: taskSummary,
  }]);

  // ... 更新其他字段
}
```

---

## 测试验证

```typescript
describe('上下文自动迁移', () => {
  test('超出限制的消息应迁移到 Memory', async () => {
    const contextManager = new ContextManager(workspacePath, {
      immediateContextRounds: 3  // 只保留3轮 = 6条消息
    });

    await contextManager.initialize('session-1', 'Test Session');

    // 添加10轮对话
    for (let i = 0; i < 10; i++) {
      contextManager.addMessage({
        role: 'user',
        content: `需求 ${i}: 创建功能模块 ${i}`
      });
      contextManager.addMessage({
        role: 'assistant',
        content: `完成了: 创建 module${i}.ts`
      });
    }

    // 验证即时上下文只保留最后3轮 = 6条消息
    expect(contextManager.getImmediateContextSize()).toBe(6);

    // 验证 Memory 包含早期需求
    const memory = contextManager.getMemoryDocument();
    const content = memory.getContent();

    // 应该至少保存了一些早期的重要信息
    expect(content.importantContext.length).toBeGreaterThan(0);
    expect(content.completedTasks.length).toBeGreaterThan(0);

    // 检查是否包含第0轮的信息
    const hasEarlyContext = content.importantContext.some(
      ctx => ctx.includes('需求 0') || ctx.includes('模块 0')
    );
    expect(hasEarlyContext).toBe(true);
  });

  test('技术决策应该被保存到 Memory', () => {
    const contextManager = new ContextManager(workspacePath);

    contextManager.addMessage({
      role: 'user',
      content: '使用 React + TypeScript + Tailwind CSS 创建前端'
    });

    const memory = contextManager.getMemoryDocument();
    const decisions = memory.getContent().keyDecisions;

    expect(decisions.length).toBeGreaterThan(0);
    expect(decisions[0].description).toContain('React');
  });
});
```

---

## 修复优先级

**P1 - 本周必做**:
- [ ] 实现 `migrateToMemory()` 方法
- [ ] 在 `addMessage()` 中调用自动迁移
- [ ] 添加测试验证迁移逻辑
- [ ] 运行现有测试确保无回归

**预期成果**:
- ✅ 长会话不再丢失早期信息
- ✅ Memory 自动积累重要上下文
- ✅ 用户体验显著改善

**预计工作量**: 3-4小时
**风险**: 低（不影响现有功能，纯新增）

---

## 总结

**实际问题**:
- 默认只保留5轮对话（10条消息）
- 超出后直接丢弃，未迁移到 Memory
- 长会话必然丢失早期关键信息

**触发概率**: **80%**（普通开发任务）
**实际影响**: **严重**（用户体验极差）
**修复难度**: **中等**（需要智能提取关键信息）

**优先级**: P1 - 本周必须修复
