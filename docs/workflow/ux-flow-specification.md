# Magi UX/UI 流程规范

> 本文档定义了主对话面板与 Worker Tab 之间的任务编排通讯交互规范

## 核心设计原则

| 原则 | 说明 |
|------|------|
| **自然语言优先** | 所有用户干预通过底部输入区域的自然语言完成 |
| **无按钮干扰** | 对话区域不显示操作按钮（Worker 卡片跳转除外） |
| **对话即控制** | 停止、重试、跳过等操作通过发送指令实现 |
| **清晰分离** | 主对话区 = 编排者叙事，Worker Tab = 执行详情 |
| **持续可交互** | 执行中仍可发送新消息，实现对话插入 |

## 双区域职责划分

| 区域 | 职责 | 内容类型 |
|------|------|---------|
| **主对话区** | 用户交互 + 编排者叙事 | 用户输入、规划说明、Worker 状态卡片、最终汇总 |
| **Worker Tab** | Worker 执行详情 | 任务说明、思考过程、工具调用、内容输出 |

---

## 消息归属与路由硬规则（不可违背）

1) **主对话区只接受编排者叙事 + Worker 节点卡片**
   - 允许：编排者分析/计划/总结/系统说明/用户消息/Worker 状态卡片（subTaskCard）
   - 禁止：Worker 直接向主对话区发送思考、工具调用、内容输出

2) **Worker Tab 只接受 Worker 自身的执行细节**
   - 允许：任务说明、思考过程、工具调用、内容输出、执行摘要
   - 禁止：编排者把执行细节写进 Worker Tab（只能下发任务说明/指令）

3) **Worker 节点卡片必须由编排者生成与更新**
   - 状态变更（执行中/等待确认/失败/完成）由编排者统一控制
   - Worker 只能上报结果，由编排者决定卡片状态变化

---

## 基础流程：单 Worker 线性执行

```text
┌─────────────────────────────────────────────────────────────────────────┐
│                          阶段 1: 用户输入                                │
│  位置: 主对话区                                                          │
│  内容: 用户消息卡片                                                       │
└─────────────────────────────────────────────────────────────────────────┘
                                   ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                       阶段 2: 编排者理解与规划                            │
│  位置: 主对话区                                                          │
│  内容:                                                                   │
│    - 思考过程（可折叠）                                                   │
│    - 任务分解说明                                                        │
│    - 执行计划概览                                                        │
└─────────────────────────────────────────────────────────────────────────┘
                                   ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                        阶段 3: 任务派发与执行                             │
│                                                                          │
│  主对话区:                          Worker Tab (Claude):                 │
│  ┌────────────────────────┐        ┌────────────────────────┐           │
│  │ 🟡 Claude              │        │ 📋 任务说明             │           │
│  │ 分析依赖               │   ──▶  │ "请分析项目依赖..."     │           │
│  │ 执行中...   → 点击跳转 │        ├────────────────────────┤           │
│  └────────────────────────┘        │ 💭 思考过程             │           │
│                                    │ (流式展开)              │           │
│                                    ├────────────────────────┤           │
│                                    │ 🔧 工具调用             │           │
│                                    │ read_file(...)         │           │
│                                    ├────────────────────────┤           │
│                                    │ 📝 分析输出             │           │
│                                    │ (Markdown 渲染)         │           │
│                                    └────────────────────────┘           │
└─────────────────────────────────────────────────────────────────────────┘
                                   ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                        阶段 4: 任务完成与汇报                             │
│                                                                          │
│  主对话区:                                                               │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ ✅ Claude                                                          │ │
│  │ 分析依赖                                                           │ │
│  │ 完成 · 发现 3 个问题                                    → 点击跳转 │ │
│  └────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
                                   ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                        阶段 5: 编排者最终总结                             │
│  位置: 主对话区                                                          │
│  内容:                                                                   │
│    - 任务执行总结                                                        │
│    - 下一步建议                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 场景 1: 多 Worker 并行执行

### 触发条件
编排者分析任务后，判断可以并行执行多个子任务（无依赖关系）

### 主对话区流程

```text
┌─────────────────────────────────────────────────────────────────────────┐
│ 👤 用户                                                                 │
│ 帮我分析这个项目的前后端依赖问题                                          │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🤖 编排者                                                               │
│ 我将安排两个 Worker 并行分析：                                           │
│ • Claude 分析前端依赖                                                    │
│ • Gemini 分析后端 API                                                   │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🟡 Claude                                                               │
│ 分析前端依赖                                                             │
│ 执行中...                                                    → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🟡 Gemini                                                               │
│ 分析后端 API                                                            │
│ 执行中...                                                    → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘

        ↓ (各自独立完成)

┌─────────────────────────────────────────────────────────────────────────┐
│ ✅ Claude                                                               │
│ 分析前端依赖                                                             │
│ 完成 · 发现 3 个循环依赖                                      → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ ✅ Gemini                                                               │
│ 分析后端 API                                                            │
│ 完成 · 5 个端点缺少错误处理                                   → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🤖 编排者                                                               │
│ 分析完成，共发现以下问题：                                                │
│                                                                         │
│ **前端依赖问题 (Claude)**                                                │
│ - components/A → utils/B → components/A 循环引用                        │
│ - ...                                                                   │
│                                                                         │
│ **后端 API 问题 (Gemini)**                                              │
│ - /api/users 缺少 try-catch                                             │
│ - ...                                                                   │
│                                                                         │
│ 需要我帮你修复这些问题吗？                                                │
└─────────────────────────────────────────────────────────────────────────┘
```

### Worker Tab 内容

**Claude Tab:**

```text
┌─────────────────────────────────────────────────────────────────────────┐
│ 📋 任务说明                                                              │
│ 分析前端项目的依赖关系，找出循环依赖和未使用的依赖                          │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 💭 思考                                                                  │
│ 我需要先读取 package.json 和 tsconfig.json 来了解项目结构...              │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🔧 read_file                                                            │
│ path: "package.json"                                                    │
│ ✅ 成功 · 1.2s                                                          │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 📝 分析结果                                                              │
│ 发现 3 个循环依赖：                                                       │
│ 1. components/A → utils/B → components/A                                │
│ 2. ...                                                                  │
└─────────────────────────────────────────────────────────────────────────┘
```

### 关键设计点

| 要点 | 说明 |
|------|------|
| 卡片布局 | 每个 Worker 独立卡片，**上下垂直排列** |
| 进度独立 | 每个 Worker 卡片有独立的状态指示 |
| 交互状态 | **关键**：卡片需实时响应交互请求（如"等待确认"），引导用户操作 |
| 完成顺序 | 先完成的卡片先更新状态，不影响其他卡片 |
| 汇总时机 | **全部完成后**编排者才输出汇总 |

---

## 场景 2: Worker 依赖链执行

### 触发条件
任务之间存在依赖关系，必须顺序执行

### 主对话区流程

```text
┌─────────────────────────────────────────────────────────────────────────┐
│ 🤖 编排者                                                               │
│ 这个任务需要分步执行：                                                    │
│ 1. Claude 先分析现有代码                                                 │
│ 2. Gemini 基于分析结果设计重构方案                                        │
│ 3. Claude 执行重构                                                       │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🟡 Claude [1/3]                                                         │
│ 分析现有代码结构                                                          │
│ 执行中...                                                    → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘

        ↓ (完成后自动触发下一步)

┌─────────────────────────────────────────────────────────────────────────┐
│ ✅ Claude [1/3]                                                         │
│ 分析现有代码结构                                                          │
│ 完成 · 识别出 5 个模块                                        → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🟡 Gemini [2/3]                                                         │
│ 设计重构方案                                                              │
│ 执行中...                                                    → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘

        ↓ (完成后继续)

┌─────────────────────────────────────────────────────────────────────────┐
│ ✅ Gemini [2/3]                                                         │
│ 设计重构方案                                                              │
│ 完成 · 生成 3 种方案                                          → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🟡 Claude [3/3]                                                         │
│ 执行重构                                                                 │
│ 执行中...                                                    → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘
```

### 关键设计点

| 要点 | 说明 |
|------|------|
| 依赖可视化 | 显示任务序号 `[1/3]` 表示执行顺序 |
| 上下文传递 | Worker B 的任务说明中包含 Worker A 的输出摘要 |
| 自动触发 | 前一个完成后自动启动下一个 |

---

## 场景 3: Worker 提问

### 触发条件
Worker 执行过程中遇到歧义，需要用户澄清

### 主对话区流程

```text
┌─────────────────────────────────────────────────────────────────────────┐
│ 🟡 Claude                                                               │
│ 重构用户模块                                                             │
│ 等待确认...                                                  → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🤖 编排者                                                               │
│ Claude 在执行过程中遇到了一个决策点：                                      │
│                                                                         │
│ 发现两种重构方案：                                                        │
│ A. Repository 模式 - 解耦清晰，代码量 +20%                                │
│ B. Service 直连 - 简单直接，耦合较紧                                      │
│                                                                         │
│ 请告诉我你倾向哪种方案，或者让 Claude 自己决定                              │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 👤 用户                                                                 │
│ 用 Repository 模式                                                      │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🟡 Claude                                                               │
│ 重构用户模块                                                             │
│ 继续执行...                                                  → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘
```

### 关键设计点

| 要点 | 说明 |
| ---- | ---- |
| 无按钮操作 | 编排者用文字描述选项，用户通过自然语言回复 |
| 状态更新 | Worker 卡片显示"等待确认"状态 |
| 上下文保持 | 用户回复后自动恢复执行 |

---

## 场景 4: 错误与恢复

### 触发条件
Worker 执行过程中发生错误（工具调用失败、LLM 超时等）

### 主对话区流程

```text
┌─────────────────────────────────────────────────────────────────────────┐
│ ❌ Claude                                                               │
│ 修改配置文件                                                             │
│ 失败 · 权限不足                                              → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🤖 编排者                                                               │
│ Claude 在修改 config.json 时遇到权限问题，无法写入文件。                    │
│                                                                         │
│ 你可以：                                                                 │
│ • 说"重试"让我再次尝试                                                    │
│ • 说"跳过"继续执行后续任务                                                │
│ • 说"回滚"撤销已完成的更改                                                │
│ • 说"停止"终止整个任务                                                    │
│ • 或者告诉我其他处理方式                                                  │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 👤 用户                                                                 │
│ 跳过这个，继续后面的                                                      │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🤖 编排者                                                               │
│ 好的，已跳过配置文件修改，继续执行后续任务。                                 │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🟡 Claude                                                               │
│ 更新类型定义                                                             │
│ 执行中...                                                    → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘
```

### 关键设计点

| 要点 | 说明 |
| ---- | ---- |
| 自然语言恢复 | 用户通过文字指令决定恢复策略 |
| 无按钮干扰 | 编排者列出可选操作但不使用按钮 |
| 状态可视化 | Worker 卡片显示失败状态 ❌ |

---

## 场景 5: 用户中断（停止）

### 触发条件
用户点击输入区域的"停止"按钮

### 主对话区流程

```text
执行中状态:
┌─────────────────────────────────────────────────────────────────────────┐
│ 🟡 Claude                                                               │
│ 重构用户模块                                                             │
│ 执行中...                                                    → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🟡 Gemini                                                               │
│ 更新 API 文档                                                           │
│ 执行中...                                                    → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 输入区域                                                      [⏹️ 停止] │
└─────────────────────────────────────────────────────────────────────────┘

用户点击 [⏹️ 停止] 后:

┌─────────────────────────────────────────────────────────────────────────┐
│ ⏹️ Claude                                                               │
│ 重构用户模块                                                             │
│ 已停止                                                       → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ ⏹️ Gemini                                                               │
│ 更新 API 文档                                                           │
│ 已停止                                                       → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🤖 编排者                                                               │
│ 已停止所有正在执行的任务。                                                │
│ • Claude: 重构用户模块 - 已完成 2/5 步骤                                  │
│ • Gemini: 更新 API 文档 - 已完成 1/3 步骤                                │
└─────────────────────────────────────────────────────────────────────────┘
```

### 关键设计点

| 要点 | 说明 |
| ---- | ---- |
| 立即停止 | 点击停止按钮后立即停止所有 Worker |
| 按钮位置 | 停止按钮仅在输入区域（发送按钮的替代状态） |
| 进度保留 | 编排者汇报各 Worker 的执行进度 |

---

## 场景 6: Todo 动态变更

### 触发条件
Worker 执行过程中发现需要新增或跳过某些 Todo

### 6.1 动态新增 Todo

```text
┌─────────────────────────────────────────────────────────────────────────┐
│ 🟡 Claude                                                               │
│ 修复登录 Bug                                                            │
│ 发现需要额外步骤...                                          → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🤖 编排者                                                               │
│ Claude 在修复过程中发现需要先重构认证模块，否则无法完成修复。               │
│ 这会新增 2 个步骤，预计多花费 2 分钟。                                     │
│                                                                         │
│ 是否同意？如果不同意，Claude 会尝试直接修复。                               │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 👤 用户                                                                 │
│ 可以                                                                    │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🟡 Claude                                                               │
│ 修复登录 Bug                                                            │
│ 正在重构认证模块 (新增步骤 1/2)...                            → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘
```

### 6.2 跳过 Todo

Worker Tab 中显示跳过原因，主对话区 Worker 卡片状态更新。

### 关键设计点

| 要点 | 说明 |
| ---- | ---- |
| 自然语言确认 | 用户通过自然语言同意或拒绝 |
| 进度更新 | Worker 卡片实时显示当前步骤 |
| 透明决策 | 编排者解释为什么需要新增步骤 |

---

## Todo 状态图例

| 状态 | 图标 | 说明 |
| ---- | ---- | ---- |
| 待执行 | ⬚ | 尚未开始 |
| 执行中 | 🟡 | 正在执行 |
| 已完成 | ✅ | 成功完成 |
| 已跳过 | ⏭️ | 主动跳过 |
| 失败 | ❌ | 执行失败 |
| 已停止 | ⏹️ | 用户中断 |

---

## UI 元素规范

### 主对话区消息卡片

| 卡片类型 | 交互方式 | 可操作元素 | 状态同步 |
| -------- | -------- | ---------- | -------- |
| 用户消息 | 只读 | 无 | - |
| 编排者消息 | 只读 | 无 | - |
| Worker 状态卡片 | 点击跳转 | 整个卡片可点击 → 跳转到对应 Tab | **关键**：需同步 `pending` 状态（如等待确认），通过颜色/图标高亮提示用户 |
| 工具调用卡片 | 展开/折叠 | 点击内容区域展开查看详情（无按钮） | - |

### Worker Tab 消息卡片

| 卡片类型 | 交互方式 |
| -------- | -------- |
| 任务说明 | 只读 |
| 思考过程 | 可折叠 |
| 工具调用 | 可展开查看输入/输出 |
| 内容输出 | 只读，支持代码高亮 |
| 完成摘要 | 只读 |

### 输入区域状态

| 状态 | 输入框 | 按钮 | 行为说明 |
| ---- | ------ | ---- | -------- |
| 空闲 | 可输入 | 发送 | 正常发送消息 |
| 执行中 | **可输入** | ⏹️/发送 | 输入为空显示停止；输入有内容显示发送（补充指令） |

#### 执行中发送消息的行为

```text
执行中状态:
┌─────────────────────────────────────────────────────────────────────────┐
│ 🟡 Claude                                                               │
│ 重构用户模块                                                             │
│ 执行中...                                                    → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 输入框: [等等，先别改 utils 目录_______________________] [⏹️ 停止]      │
└─────────────────────────────────────────────────────────────────────────┘

用户按回车发送后:

┌─────────────────────────────────────────────────────────────────────────┐
│ 🟡 Claude                                                               │
│ 重构用户模块                                                             │
│ 执行中...                                                    → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 👤 用户                                                                 │
│ 等等，先别改 utils 目录                                                  │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🤖 编排者                                                               │
│ 收到，我会通知 Claude 暂停对 utils 目录的修改...                          │
└─────────────────────────────────────────────────────────────────────────┘
```

**补充指令语义（明确规则）**
- 执行中新增输入属于**补充指令**：不会丢弃当前任务
- 默认不打断当前步骤，仅在**下一决策点**生效
- 如需立即打断，必须显式指令（如“停止并改为…”）

#### 输入区域按钮状态机

```text
┌─────────────────────────────────────────────────────────────────────────┐
│                          按钮状态转换                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  空闲状态:                                                               │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │ 输入框为空      → [发送] 按钮禁用（灰色）                          │   │
│  │ 输入框有内容    → [发送] 按钮启用                                  │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  执行中状态:                                                             │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │ 输入框为空      → [⏹️ 停止] 按钮                                   │   │
│  │ 输入框有内容    → [发送] 按钮（发送后清空，按钮变回停止）            │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 高频发送限制（强约束）

| 场景 | 处理方式 |
| ---- | -------- |
| 执行中高频补充 | **限频 1 秒 1 条**，超出直接提示并不发送 |
| 空闲状态高频发送 | **限频 300ms 1 条**，超出直接提示并不发送 |
| 连续超限 | 弹出提示：“发送过快，请稍后再试” |

```text
高频发送:
  消息1: 允许发送
  消息2: <1s 内发送 → 拒绝并提示
  消息3: <1s 内发送 → 拒绝并提示
```

#### 关键设计点

| 要点 | 说明 |
| ---- | ---- |
| 输入不阻塞 | 执行中用户仍可输入新消息 |
| 补充语义 | 新消息作为补充指令，不丢弃当前任务 |
| 上下文感知 | 编排者理解这是执行中的追加指令，必要时调整 Worker 行为 |
| 按钮双态 | 输入框为空显示停止，有内容显示发送 |
| 限频规则 | 超频直接拒绝并提示，不进入队列 |

---

## 消息类型完整列表

| 消息类型 | source | 显示位置 | 用途 |
| -------- | ------ | -------- | ---- |
| `USER_INPUT` | user | 主对话区 | 用户输入 |
| `ORCHESTRATOR_THINKING` | orchestrator | 主对话区 | 编排者思考（可折叠） |
| `ORCHESTRATOR_PLAN` | orchestrator | 主对话区 | 任务规划说明 |
| `ORCHESTRATOR_RESPONSE` | orchestrator | 主对话区 | 编排者回复/提问 |
| `ORCHESTRATOR_SUMMARY` | orchestrator | 主对话区 | 最终汇总 |
| `WORKER_STATUS_CARD` | orchestrator | 主对话区 | Worker 状态卡片（可点击跳转） |
| `WORKER_INSTRUCTION` | orchestrator | Worker Tab | 任务说明 |
| `WORKER_THINKING` | worker | Worker Tab | 思考过程 |
| `WORKER_TOOL_USE` | worker | Worker Tab | 工具调用 |
| `WORKER_OUTPUT` | worker | Worker Tab | 内容输出 |
| `WORKER_SUMMARY` | worker | Worker Tab | 执行摘要 |

---

## 设计原则检查清单

| 检查项 | 状态 |
| ------ | ---- |
| 无操作按钮干扰对话区 | ✅ |
| 所有干预通过自然语言 | ✅ |
| Worker 卡片可点击跳转 | ✅ |
| 停止按钮仅在输入区域 | ✅ |
| 每个 Worker 独立卡片（上下布局） | ✅ |
| 主对话区保持高层叙事 | ✅ |
| Worker Tab 显示完整详情 | ✅ |
| 执行中仍可输入发送新消息 | ✅ |

---

## 术语表（Glossary）

| 术语 | 定义 |
| ---- | ---- |
| **编排者（Orchestrator）** | 负责理解用户意图、分配任务、协调 Worker 的核心 AI。所有 Worker 间通讯均通过编排者中转 |
| **Worker** | 执行具体任务的 AI Agent（如 Claude/Codex/Gemini），只负责执行，不直接与其他 Worker 通讯 |
| **主对话区** | 用户与编排者交互的主界面，展示高层叙事（规划、状态卡片、汇总） |
| **Worker Tab** | 每个 Worker 独立的执行详情面板，展示思考、工具调用、内容输出等 |
| **Worker 状态卡片** | 主对话区展示的 Worker 执行状态摘要卡片，可点击跳转到对应 Tab |
| **任务说明（Instruction）** | 编排者派发给 Worker 的任务描述，显示在 Worker Tab 顶部 |
| **补充指令** | 执行中用户发送的追加消息，不中断当前任务，在下一决策点生效 |
| **依赖链执行** | 任务之间存在依赖关系，必须按顺序执行（A→B→C） |
| **并行执行** | 多个 Worker 同时执行无依赖关系的任务（A\|\|B） |
| **上下文传递** | 编排者摘要前序 Worker 输出，注入后续 Worker 的任务说明 |
| **限频** | 防止高频发送的速率限制机制（执行中 1秒/条，空闲 300ms/条） |
| **决策点** | Worker 执行过程中的关键节点，编排者可在此注入补充指令（见下方详细定义） |

---

## 补充规则详解

### 决策点定义（补充指令生效时机）

**决策点**是 Worker 执行过程中编排者可以安全注入用户补充指令的节点：

| 决策点类型 | 触发时机 | 示例 |
| ---------- | -------- | ---- |
| **工具调用前** | Worker 准备执行工具调用时 | 读取文件前、执行命令前 |
| **步骤边界** | 当前 Todo 完成，准备执行下一个 | 完成"分析代码"，准备"生成方案" |
| **思考完成** | 一轮思考结束，准备输出或行动 | 思考后决定下一步 |
| **等待用户确认** | Worker 主动请求用户输入 | 方案选择、风险确认 |

```text
补充指令生效流程:

  用户发送: "别改 utils 目录"
              ↓
  编排者接收并暂存
              ↓
  Worker 继续当前步骤（不中断）
              ↓
  Worker 到达决策点（如：准备修改下一个文件）
              ↓
  编排者注入补充指令: "跳过 utils 目录下的文件"
              ↓
  Worker 调整后续行为
```

#### 例外：立即打断

- 用户显式指令（如"停止"、"立即停止"、"取消"）会立即中断当前步骤
- 点击停止按钮会立即终止所有 Worker

---

### 并行执行汇总策略

| 场景 | 汇总时机 | 行为 |
| ---- | -------- | ---- |
| 全部成功 | 所有 Worker 完成后 | 编排者输出统一汇总 |
| 部分失败 | 失败 Worker 立即汇报，成功者继续 | 编排者先报告失败，询问是否继续等待 |
| 长时间执行 | 任一 Worker 完成后 | 可选：编排者输出阶段性进展（"Claude 已完成，等待 Gemini..."） |

```text
阶段性汇报示例（可选启用）:

┌─────────────────────────────────────────────────────────────────────────┐
│ ✅ Claude                                                               │
│ 分析前端依赖                                                             │
│ 完成 · 发现 3 个循环依赖                                      → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🟡 Gemini                                                               │
│ 分析后端 API                                                            │
│ 执行中...                                                    → 点击跳转 │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 🤖 编排者                                                               │
│ Claude 已完成前端分析，发现 3 个循环依赖。                                 │
│ Gemini 仍在分析后端 API，预计还需 1-2 分钟。                              │
│ 是否需要我先汇报 Claude 的结果？                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

### 依赖链中断与恢复

当依赖链执行中某步骤失败时：

| 失败位置 | 影响范围 | 恢复选项 |
| -------- | -------- | -------- |
| 中间步骤 | 后续步骤无法执行 | 重试 / 跳过 / 回滚 / 终止 |
| 最后步骤 | 仅当前步骤失败 | 重试 / 跳过 / 回滚 |

```text
依赖链中断示例:

  ✅ Claude [1/3] 分析代码结构 → 完成
  ❌ Gemini [2/3] 设计重构方案 → 失败（API 超时）
  ⬚ Claude [3/3] 执行重构     → 待执行（被阻塞）

┌─────────────────────────────────────────────────────────────────────────┐
│ 🤖 编排者                                                               │
│ Gemini 在设计重构方案时遇到问题（API 超时）。                              │
│                                                                         │
│ 当前进度：                                                               │
│ • [1/3] 分析代码结构 ✅ 已完成                                           │
│ • [2/3] 设计重构方案 ❌ 失败                                             │
│ • [3/3] 执行重构     ⬚ 待执行（依赖第 2 步）                             │
│                                                                         │
│ 你可以：                                                                 │
│ • 说"重试"让 Gemini 再次尝试                                             │
│ • 说"换 Claude 来做"让其他 Worker 接手                                   │
│ • 说"跳过设计，直接重构"跳过当前步骤                                      │
│ • 说"回滚"撤销第 1 步的分析结果                                          │
│ • 说"停止"终止整个任务                                                   │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 回滚策略

| 操作类型 | 可回滚性 | 回滚方式 |
| -------- | -------- | -------- |
| 文件修改 | ✅ 可回滚 | Git stash / 备份还原 |
| 文件删除 | ⚠️ 部分可回滚 | Git restore（如有 Git） |
| 外部 API 调用 | ❌ 不可回滚 | 只能记录日志供人工处理 |
| 数据库修改 | ⚠️ 视情况 | 需事务支持或备份 |

#### 回滚原则

1. 编排者在执行前评估任务的可回滚性
2. 高风险操作（不可逆）需显式确认
3. 回滚失败时，编排者报告已回滚和未回滚的部分
